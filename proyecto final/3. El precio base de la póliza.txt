3. El precio base de la póliza recibe recargos según rangos de edad.
Este recargo se aplica:

A) Al asegurado (siempre)

B) Al cónyuge (solo si está casado)

Los rangos son:

*18 a 24 años → recargo 10% del precio base

*25 a 49 años → recargo 20% del precio base

*50 o más → recargo 30% del precio base

El recargo del asegurado y del cónyuge se calcula por separado (cada uno sobre el mismo precio_base) y se suman al recargo total.

Líneas de Código:
var nombre = prompt ("Ingrese su nombre, por favor")
var edad = prompt ("´¿Cuantos años tiene? Ingrese solamente números")


var edad_numero = parseInt(edad)

if  (edad_numero < 18) {

alert( " No se Puede Cotizar: el Asegurado debe ser mayor de edad (18+).")
 throw new Error ("Asegurado menor de Edad")

} 

var edad_conyuge_numero = 0
if ( "SI" == casado.toUppercase()){
var edad conyuge = prompt ("¿ Qué edad tiene su esposo/a? Ingese solamente números")
edad_conyuge_numero = parseInt(edad_conyuge)
}

var hijos = prompt("¿Tiene hijos o hijas? (si/no)")

var cantidad_hijos = 0
if ("SI" == hijos.toUpperCase()) {
cantidad_hijos = parseInt(prompt("Cuántos hijos o hijas tiene? Ingrese solamente números"))
}
if (edad_numero >= 18 && edad_numero < 25) {
recargo = precio_base * edad_18
recargo_total = recargo_total + recargo
} 
else if (edad_numero >= 25 && edad_numero < 50) {
recargo = precio base + edad_25
recargo total = recargo_total + recargo
}
else if (edad_numero >= 50){

recargo = precio_base * edad_50
recargo_total = recargo_total + recargo
}

if ("SI" == casado.toUpperCase()){

if (edad_conyuge_numero >= 18 && edad_conyuge_numero < 25){
  recargo = precio_base * casado_18

}else if (edad_conyuge_numero >= 25 && edad_conyuge_numero < 50){
recargo = precio base * casado_25
recargo_total = recargo_total + recargo
}else if(edad_conyuge_numero >= 50){
recargo = precio_base * casado_50
recargo_total = recargo_total + recargo
}
}


*Se evalúa la edad del asegurado en 3 rangos y se calcula el recargo correspondiente usando el precio base.

*Se suma el recargo obtenido a recargo_total.

*Si el usuario está casado, se repite el mismo procedimiento para la edad del cónyuge.

*Ambos recargos (asegurado + cónyuge) se acumulan en recargo_total.